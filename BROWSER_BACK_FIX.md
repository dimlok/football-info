# 🔙 Browser Back/Forward Transitions - Решение

## 🔍 Проблема:

View Transitions работают при клике по ссылкам (через TransitionLink), но НЕ работают при использовании кнопки "назад" в браузере.

## ⚙️ Что было сделано:

### 1. Добавлен future flag в React Router

```typescript
const router = createBrowserRouter([...], {
  future: {
    unstable_viewTransition: true, // Экспериментальная поддержка
  },
})
```

### 2. Добавлен Navigation API listener

В `App.tsx` добавлен обработчик который перехватывает ВСЮ browser navigation (включая back/forward) и оборачивает в View Transition.

## 🧪 Как протестировать:

### Шаг 1: Откройте DevTools Console

```
F12 → Console
```

### Шаг 2: Проверьте поддержку Navigation API

В консоли должны увидеть при загрузке страницы:

- ✅ Ничего (API поддерживается)
- ⚠️ "Navigation API не поддерживается" (API не доступен)

### Шаг 3: Тест browser back

1. Кликните Teams → откройте любую команду
2. Нажмите browser back кнопку (←)
3. **Проверьте консоль:**
   - Видите "🔄 Navigation event:"? → Navigation API работает!
   - Видите "🎬 Browser navigation - View Transition запущен!"? → Transitions включены!
   - Ничего не видите? → Navigation API не перехватывает SPA navigation

## 🎯 Проблемы и решения:

### Проблема 1: Navigation API не работает с React Router SPA

**Причина:** Navigation API intercept работает только для "настоящей" browser navigation, а React Router использует history API для SPA роутинга.

**Решение:** React Router v7 имеет встроенную поддержку через `unstable_viewTransition` флаг. Мы его включили!

### Проблема 2: CSS `@view-transition { navigation: auto; }` не работает для SPA

**Причина:** Это CSS правило работает только для multi-page applications где каждый переход = новая страница загружается.

**Решение:** Мы используем JavaScript-based transitions через `TransitionLink` и Navigation API.

### Проблема 3: Transitions всё равно не работают при browser back

**Возможные причины:**

1. ❌ React Router v7.11 не поддерживает `unstable_viewTransition` (слишком старая/новая версия)
2. ❌ Navigation API не доступен в вашем браузере
3. ❌ Navigation API не перехватывает SPA navigation

**Альтернативное решение:**

Если ничего не работает, можем использовать **третий способ** - перехватывать popstate события напрямую и применять transitions вручную.

## 🔧 Альтернативное решение (если current не работает):

Добавьте в `App.tsx`:

```typescript
useEffect(() => {
	if (!document.startViewTransition) {
		return
	}

	let isTransitioning = false

	const handlePopState = () => {
		if (isTransitioning) {
			return
		}

		isTransitioning = true
		console.log('🔙 PopState event - Browser back/forward!')

		// Даем React Router время на обновление DOM
		requestAnimationFrame(() => {
			requestAnimationFrame(() => {
				console.log('🎬 Applying View Transition for browser back!')
				document
					.startViewTransition(() => {
						// Transition уже произошел, просто анимируем
					})
					.finished.finally(() => {
						isTransitioning = false
					})
			})
		})
	}

	window.addEventListener('popstate', handlePopState)

	return () => {
		window.removeEventListener('popstate', handlePopState)
	}
}, [])
```

**Проблема с этим подходом:** popstate срабатывает ПОСЛЕ того как React Router уже обновил DOM, поэтому transitions может выглядеть странно.

## 📊 Статус решения:

### ✅ Работает сейчас:

- Transitions при клике по ссылкам (TransitionLink)
- Transitions на всех страницах
- 800ms анимация, очень заметная

### ⚠️ В процессе:

- Transitions при browser back/forward
- Navigation API integration
- React Router v7 future flag

### 🔍 Нужно протестировать:

1. Видите ли вы логи "🔄 Navigation event:" в консоли?
2. Работает ли transition при browser back?

## 🚀 Следующие шаги:

1. **Протестируйте текущее решение:**

   - Откройте http://localhost:5174/
   - Перейдите Teams → Team Detail
   - Нажмите browser back
   - Проверьте консоль - есть логи?
   - Видите transition?

2. **Если не работает:**

   - Сообщите результаты
   - Попробуем альтернативное решение с popstate

3. **Если работает:**
   - 🎉 Готово!
   - Можно убрать console.log'и для production

## 💡 Важно понять:

Browser back/forward transitions - это **сложная задача** в SPA, потому что:

- React Router управляет роутингом через JavaScript
- Browser не знает о "transitions" между JavaScript состояниями
- Navigation API еще экспериментальный
- Каждый фреймворк решает это по-своему

Мы пробуем **самый современный подход** через Navigation API + React Router v7 flags. Если не работает, есть workaround'ы!

---

**Протестируйте и сообщите результаты!** 🧪

